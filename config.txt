"""
config.py - URLs, credentials, proxy.
HSBC headers are defined directly in each client function (not via env).
"""
import os

# ---- UPI ----
UPI_BASE_URL    = os.getenv("UPI_BASE_URL",    "https://upi-uat-mob.hsbc.co.in")
UPI_LOGIN_PATH  = os.getenv("UPI_LOGIN_PATH",  "/sha/api/auth/login")
UPI_SECURE_PATH = os.getenv("UPI_SECURE_PATH", "/mha/secure/sha/check")
UPI_USERNAME    = os.getenv("UPI_USERNAME",    "hsbc")
UPI_PASSWORD    = os.getenv("UPI_PASSWORD",    "hsbcuat2026")
UPI_AES_KEY_HEX = os.getenv("UPI_AES_KEY_HEX","")

# ---- HSBC Hub API URLs ----
TXN_HISTORY_URL = os.getenv(
    "TXN_HISTORY_URL",
    "https://cbil-in-hbap-hub-dd-trans-history-enq-3.hsbc-cbsa-hub-cert.svc.apiapg02.shp.ape1.pre-prod.aws.cloud.hsbc"
    "/api/v3/account/demand-deposit-transaction-history",
)
ACCT_HOLD_URL = os.getenv(
    "ACCT_HOLD_URL",
    "https://cbil-in-hbap-hub-add-acct-hld-pst-2.hsbc-cbsa-hub-cert.svc.aplapg02.shp.ape1.pre-prod.aws.cloud.hsbc"
    "/api/accounts/apply-account-holds",
)
DDA_URL = os.getenv(
    "DDA_URL",
    "https://cbil-in-hbap-hub-demand-deposit-acct-enq-5.hsbc-cbsa-hub-cert.svc.aplapg02.shp.ape1.pre-prod.aws.cloud.hsbc"
    "/api/v5/accounts/demand-deposit-account",
)

# ---- Proxy ----
# Trailing slash stripped automatically so http://proxy.test/ also works.
_raw_proxy = os.getenv("PROXY_URL", "")
PROXY_URL  = _raw_proxy.rstrip("/") if _raw_proxy else ""

# ---- HTTP timeouts ----
HTTP_CONNECT_TIMEOUT = float(os.getenv("HTTP_CONNECT_TIMEOUT", "10"))
HTTP_READ_TIMEOUT    = float(os.getenv("HTTP_READ_TIMEOUT",    "60"))

# ---- UPI token cache ----
TOKEN_REFRESH_LEEWAY = int(os.getenv("TOKEN_REFRESH_LEEWAY_SEC", "60"))
TOKEN_DEFAULT_TTL    = int(os.getenv("TOKEN_DEFAULT_TTL_SEC",    "900"))
